{
  "lock": false,
  "tasks": {
    "dev": "denon dev",
    "lint": "deno lint && deno run --allow-env --allow-read --allow-sys tools/textlint.ts *",
    "lint:watch": "denon lint",
    "start": "deno run -A --import-map import_map.json src/main.ts",
    "build": "deno compile -A -o main src/main.ts && chmod +x ./main",
    "start:product": "./main"
  }
}